# Companion code for "A Tutorial on Martingales"

This repo holds the code, experiments, and figures for the blog post at
`content/research/martingale_tutorial.md` in the `fernandopalafox.github.io`
repo (Quartz-based site, Research section). No writing happens here — the
post's prose lives and is edited in that repo, following its
`content/research/CLAUDE.md` conventions.

## Reference material

- `references/rsta.2022.0143.pdf` (untracked, ignored via `.gitignore`).

## Figures

- Generate and iterate on figures here.
- Once a figure is final, copy it into `content/research/media/` in the
  website repo and reference it from the post with:
  `<img src="media/name.ext" alt="" style="width:75%">`
- Don't embed figures anywhere else in the website repo.

## Status

Local-only as of 2026-07-09, not yet pushed to a remote. No code or
structure has been decided on yet — don't assume a language, package
manager, or directory layout beyond what's actually present.
