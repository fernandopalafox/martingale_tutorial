# Companion code for "A Tutorial on Martingales"

This repo holds the code, experiments, and figures for Part 2 of the blog
post, at `content/research/martingale2.md` in the `fernandopalafox.github.io`
repo (Quartz-based site, Research section). No writing happens here — the
post's prose lives and is edited in that repo, following its
`content/research/CLAUDE.md` conventions.

## Reference material

- `references/rsta.2022.0143.pdf` (untracked, ignored via `.gitignore`).

## Figures

- Generate and iterate on figures here.
- Once a figure is final, copy it into `content/media/` (a sibling of
  `content/research/`, not a subdirectory of it) in the website repo and
  reference it from the post with:
  `<img src="media/name.ext" alt="" style="width:75%">`
- Don't embed figures anywhere else in the website repo.

## Status

Pushed to `https://github.com/fernandopalafox/martingale_tutorial`, linked
from the post as the companion code repo.
